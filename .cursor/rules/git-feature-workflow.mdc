---
description: Santako Music Git workflow - feature → develop → main → deploy
alwaysApply: true
---

# Santako Music Git Workflow

## When user says "merge feature"

1. Check branch:
   git branch

2. Commit changes (if needed):
   git status
   git add .
   git commit -m "feat: short description"

3. Merge into develop:
   git checkout develop
   git pull
   git merge <current-branch> --no-edit

4. Push develop:
   git push origin develop

⚠️ Never merge feature directly into main.

---

## When user says "prepare production"

1. Checkout develop:
   git checkout develop
   git pull

2. Merge into main:
   git checkout main
   git pull
   git merge develop --no-edit

3. Push main:
   git push origin main

Netlify auto-deploys on push to main.

---

## When user says "new feature X"

git checkout develop
git pull
git checkout -b feature/<kebab-case-name>

---

## When user says "preview"

npm run build
netlify deploy --dir=dist

---

## Commit Format (mandatory)

type: short description

Allowed types:
feat
fix
refactor
style
content
docs
chore